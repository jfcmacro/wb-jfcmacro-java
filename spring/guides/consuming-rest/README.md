# Consuming Rest Service

## Create

```bash
spring init --artifact-id "consuming-rest" --group-id "org.jfcmc" --version "0.0.1" --description "Consuming Rest Services" --package-name "org.jfcmc.spring.consrestserv" --name "consuming-rest" --boot-version 4.0.3 --build maven --language java --java-version 17 --dependencies "spring-webclient"  consuming-rest/
```
